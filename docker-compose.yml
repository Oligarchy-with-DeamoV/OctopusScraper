services:
  octopus-service:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    image: octopus-scraper:latest
    container_name: octopus-service
    ports:
      - "8001:8000"
    environment:
      - NOTION_API_KEY=${NOTION_API_KEY}
      - NOTION_SCRAPERS_DATABASE_ID=${NOTION_SCRAPERS_DATABASE_ID}
      - NOTION_CONTENT_DATABASE_ID=${NOTION_CONTENT_DATABASE_ID}
      # Cache config, will affect content refresh
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FORMAT=${LOG_FORMAT:-plain}
      - CONFIG_REFRESH_INTERVAL=${CONFIG_REFRESH_INTERVAL:-300}
      - USE_TASK_MANAGER=${USE_TASK_MANAGER:-true}
      - TASK_MANAGER_MAX_CONCURRENT=${TASK_MANAGER_MAX_CONCURRENT:-3}
      - TASK_MANAGER_MAX_QUEUE_SIZE=${TASK_MANAGER_MAX_QUEUE_SIZE:-1000}
      - SERVICE_HOST=${SERVICE_HOST:-0.0.0.0}
      - SERVICE_PORT=${SERVICE_PORT:-8000}
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - OCTOPUS_LOG_LEVEL=INFO
      # RSSHub fetcher HTTP timeouts in seconds. Defaults match the
      # historical hard-coded values; override via .env to tune.
      - RSSHUB_CONNECT_TIMEOUT=${RSSHUB_CONNECT_TIMEOUT:-10}
      - RSSHUB_READ_TIMEOUT=${RSSHUB_READ_TIMEOUT:-2000}

    # 健康检查配置（slim 镜像无 curl，使用 Python 内置检查）
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/liveness')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
        reservations:
          memory: 256M
          cpus: "0.25"

    restart: unless-stopped

    volumes:
      - logs:/app/logs

    networks:
      - octopus-network

  # 定时调度服务：基于 cron，编辑 scheduler/crontab 即可管理所有定时任务
  scheduler:
    image: alpine:3.19
    container_name: octopus-scheduler
    restart: unless-stopped
    depends_on:
      octopus-service:
        condition: service_healthy
    volumes:
      - ./scheduler/crontab:/etc/scheduler/crontab:ro
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache curl > /dev/null 2>&1
        cp /etc/scheduler/crontab /var/spool/cron/crontabs/root
        chmod 600 /var/spool/cron/crontabs/root
        echo "Scheduler started with cron jobs:"
        cat /var/spool/cron/crontabs/root
        crond -f -l 2
    networks:
      - octopus-network

  rsshub:
    # if used default rsshub use image below
    # image: diygod/rsshub:chromium-bundled
    image: info-channel:1.0.0
    restart: always
    ports:
      - "1200:1200"
    environment:
      TZ: Asia/Shanghai
      NODE_ENV: production
      CACHE_TYPE: redis
      REDIS_URL: "redis://redis:6379/"
      XUEQIU_TOKEN: ${XUEQIU_TOKEN}
      XUEQIU_RATE_WINDOW_SECONDS: ${XUEQIU_RATE_WINDOW_SECONDS:-60}
      XUEQIU_RATE_LIMIT: ${XUEQIU_RATE_LIMIT:-25}
      XUEQIU_DELAY_MIN_MS: ${XUEQIU_DELAY_MIN_MS:-500}
      XUEQIU_DELAY_MAX_MS: ${XUEQIU_DELAY_MAX_MS:-1500}
      XUEQIU_SCAN_POST_REPLIES: ${XUEQIU_SCAN_POST_REPLIES:-true}
      CACHE_CONTENT_EXPIRE: ${CACHE_CONTENT_EXPIRE:-43200}
      CACHE_EXPIRE: ${CACHE_EXPIRE:-300}
      LOGGER_LEVEL: ${LOGGER_LEVEL:-info}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1200/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - redis
    networks:
      - octopus-network

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 5s
    networks:
      - octopus-network

  # Vector 日志监控服务：
  #   - 监控 rsshub 的 error 级别日志
  #   - 监控 octopus-service 的 "Task failed" 事件（task_manager 任务失败时发出）
  #   命中即通过飞书 Webhook 推送告警卡片。
  vector-alert:
    image: timberio/vector:latest-alpine
    container_name: vector-alert
    environment:
      - TZ=Asia/Shanghai
      - FEISHU_WEBHOOK_URL=${FEISHU_WEBHOOK_URL}
      - VECTOR_LOG=${VECTOR_LOG:-info}
    volumes:
      - ./vector.toml:/etc/vector/vector.toml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - octopus-network
    command: ["--config", "/etc/vector/vector.toml"]
    restart: unless-stopped
    depends_on:
      - rsshub
      - octopus-service

networks:
  octopus-network:
    driver: bridge

volumes:
  logs:
  redis-data:
  prometheus-data:
