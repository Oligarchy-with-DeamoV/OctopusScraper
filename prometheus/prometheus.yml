global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: octopus-scraper
    metrics_path: /metrics
    static_configs:
      - targets:
          - octopus-service:8000
